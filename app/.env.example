# Optional override. Leave empty for same-origin /api requests.
VITE_API_BASE_URL=

# household: bootstraps safe configuration from the Home Service
# demo: uses tracked synthetic client configuration
VITE_EY_MODE=household
