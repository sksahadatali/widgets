# Optional override. Leave empty for same-origin /api requests.
VITE_API_BASE_URL=

# household: requires app/src/config/household.local.json
# demo: uses the public household.example.json configuration
VITE_EY_MODE=household
